Destructure the first two features and the remaining features as a new array using the spread operator.

const car = { model: "Sedan", features: ["Sunroof", "Leather seats", "Bluetooth", "Backup Camera", "Navigation"] };
